meta {
  name: Providers / List
  type: http
  seq: 4
}

post {
  url: {{base_url}}/panel/providers/list
  body: json
  auth: inherit
}

headers {
  Client-Token: {{client_token}}
  Auth-Token: {{auth_token}}
}

body:json {
  {
      "page": 1,
      "per_page": 10
  }
}

settings {
  encodeUrl: true
}

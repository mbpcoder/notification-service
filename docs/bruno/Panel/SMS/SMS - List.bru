meta {
  name: SMS / List
  type: http
  seq: 1
}

post {
  url: {{base_url}}/panel/sms/list
  body: json
  auth: inherit
}

headers {
  Client-Token: {{client_token}}
  Auth-Token: {{auth_token}}
}

body:json {
  {
      "page": 1,
      "per_page": 10
  }
}

settings {
  encodeUrl: true
}
